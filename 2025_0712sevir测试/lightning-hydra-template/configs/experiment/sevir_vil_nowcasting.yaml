# @package _global_

defaults:
  - override /data: sevir_vil.yaml
  - override /model: convlstm_vil.yaml
  - override /trainer: default.yaml

name: "sevir_vil_nowcasting"
tags: ["sevir", "vil", "nowcasting"]
seed: 42
logger: null  # 简化测试

trainer:
  max_epochs: 10
  accelerator: gpu
  devices: 1
  precision: 16

# 可以通过命令行覆盖这些参数进行实验
data:
  batch_size: 2  # 可以尝试更大的批次

model:
  hidden_dims: [32]  # 可以尝试更大的模型